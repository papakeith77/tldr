# TLDR iOS (SwiftUI starter)

This is **starter code** you can paste into a new Xcode project:

- Xcode → New Project → iOS App → SwiftUI
- Replace ContentView.swift with the file below
- Set `baseURL` to your deployed TLDR web app (Vercel) or your dev machine IP.
